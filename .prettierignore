node_modules
dist
.next
.nx
coverage
pnpm-lock.yaml
**/generated
database/migrations
# Rewritten by `next build` on every run, double-quoted, and marked "should not
# be edited". Formatting it makes `format:check` fail after the next build.
# Already ignored the same way in the portal's eslint.config.mjs.
next-env.d.ts
# Helm templates are Go templates, not valid YAML on their own.
infrastructure/helm/**/templates
# Portail AGEROUTE : arrivé avec sa propre chaîne, en JavaScript nu et sans
# dépendance, comme `apps/web/ageroute` que pnpm-workspace.yaml traite déjà à
# part. Le reformater noierait le code d'origine sous un diff mécanique sans
# rien apprendre à personne. À retirer d'ici le jour où ces deux projets
# rejoignent la chaîne TypeScript du dépôt.
apps/web/portail-opportunites
services/integrations/relais-portail
docs/cahier-des-charges/portail-opportunites
infrastructure/docker/portail-opportunites
infrastructure/sharepoint/portail-opportunites
