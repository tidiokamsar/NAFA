apiVersion: v1
kind: Service
metadata:
  name: {{ include "nafa-service.fullname" . }}
  labels:
    {{- include "nafa-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nafa-service.selectorLabels" . | nindent 4 }}
