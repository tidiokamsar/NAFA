export default function Home() {
  return (
    <main>
      <h1>NAFA — Admin Portal</h1>
      <p>
        Reference Next.js application shell. It carries no business logic: it
        exists so the toolchain (build, lint, typecheck, Docker image) is proven
        end to end before feature work starts.
      </p>
      <ul>
        <li>
          Health endpoint: <code>/api/health</code>
        </li>
      </ul>
    </main>
  );
}
